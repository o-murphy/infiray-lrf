## Instalation
```shell
git clone https://github.com/o-murphy/lr2000_lr3000
cd lr_2000_lr_3000
python3 -m venv venv
. venv/bin/activate
pip install -r requirements.txt
```

## Usage
```shell
# for sniffing lrf
python3 -m sniffer

# for sending commands
python3 -m sender
```
